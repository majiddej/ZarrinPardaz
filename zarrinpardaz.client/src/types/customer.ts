export interface Customer {
  id: number;
  name: string;
  email: string;
  phone: string;
  address: string;
}

export interface NewCustomer {
  name: string;
  email: string;
  phone: string;
  address: string;
}